BeanFactory: it is subset of Application Context which will implement lazy loading. which means it
             will create bean when it is used for very first time. it will also manage bean life cycle.

